{:paths ["src" "test"]
 :tasks
 {test {:doc "Run kizashi murakumo actor boundary tests"
        :task (do
                (require 'clojure.test
                         'kizashi.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'kizashi.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
